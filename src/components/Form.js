import { Collapse } from 'antd';
const { Panel } = Collapse;

const Form = (props) => {
    return (
        <div>
            <p>This is the form!</p>
            <Collapse>
                const {Panel} = Collapse;
            </Collapse>
        </div>
    )
}

export default Form;